export interface CollaboratorItemType {
  name: string;
  photoUrL: string;
  actif: boolean;
  id: string;
}

export interface TokenItemType {
  name: string;
  photoUrL: string;
  version: string;
  id: string;
}
